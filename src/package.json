{
  "name": "copy-shorturl",
  "scripts": {
    "build": "web-ext build --ignore-files dist/ webpack.*",
    "test": "web-ext run --verbose"
  },
  "devDependencies": {},
  "dependencies": {
    "npm": ">=8.11.0",
    "web-ext": "^6.5.0"
  }
}
